# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing these this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: Thư viện truyện dành cho LGBT
email: admin@thuvientruyen.info
description: Nơi tìm lại cảm xúc!
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://thuvientruyen.info" # the base hostname & protocol for your site
disqus_url: "https://invincip.github.io/tvt/disqus.html"
lang: vi
collections:
  subposts:
    output: true
gems:
  - jekyll-paginate
  - jekyll-sitemap
  - jekyll-feed
paginate: 10
paginate_path: "/page/:num/"
archives:
  tag:
    display: thẻ
    path: /the/
  category:
    display: thể loại
    path: /
  contributor:
    display: người đăng
    path: /nguoi-dang/
  author:
    display: tác giả
    path: /tac-gia/
    
# Build settings
markdown: kramdown
exclude: [Gemfile, Gemfile.lock, firebase.json, vendor]
sass:
  style: compressed
